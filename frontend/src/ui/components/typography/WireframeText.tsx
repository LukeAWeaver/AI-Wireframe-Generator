import { Typography, TypographyProps } from '@mui/material';
import { styled } from '@mui/material/styles';

// Styled component for wireframe section headings
export const WireframeSectionHeading = styled(Typography)<TypographyProps>(() => ({
  marginBottom: 16,
  textAlign: 'center',
})); 